# Firebase (Auth + Firestore เท่านั้น — ไม่ใช้ Firebase Storage)
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=

# Vercel Blob (เก็บไฟล์เอกสารประชุม)
# สร้างที่: Vercel Dashboard → Storage → Blob → Create
# แล้วคัดลอก BLOB_READ_WRITE_TOKEN
BLOB_READ_WRITE_TOKEN=

# Firebase Console ที่ต้องเปิด:
# - Authentication → Email/Password
# - Firestore Database (+ วาง firestore.rules)
# ไม่จำเป็นต้องเปิด Firebase Storage
